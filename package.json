# package.json
# ============================================================
# Usage:
#   npm install
#   npx wrangler dev
# ============================================================

{
  "name": "server-approval",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
